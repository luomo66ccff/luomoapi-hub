# Security Policy

LuomoAPI Hub 处在调用者与上游秘密之间。部署者必须保持以下边界：

- 8790 只监听回环地址，外部流量通过 TLS；
- 只有管理员可以创建或修改路由；
- 上游只使用 HTTPS，域名及解析 IP 均经过审核；
- API key、会话密钥、SMTP 和上游凭据只存在于环境变量或秘密管理器；
- 匿名路由默认关闭，并逐条评估滥用成本；
- 数据库和使用日志按敏感数据处理。

发现 key 绕过、scope 绕过、SSRF、配额绕过或日志泄密，请使用 GitHub 私密安全公告报告。不要在公开 issue 中提交可用 key、Cookie、用户数据或真实上游响应。
